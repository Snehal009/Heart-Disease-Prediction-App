

# ❤️ Heart Disease Prediction App 

A simple and interactive **Streamlit web application** that predicts the risk of **Heart Disease** using a Logistic Regression machine learning model.
Users can input patient health details, and the app provides a real-time prediction based on trained data.


## 🚀 Features

* 🖥️ Clean & modern **Streamlit UI**
* 🔍 Predicts **heart disease risk** using ML
* 📊 Uses **scaler** + **one-hot encoded columns** for correct model input
* 📁 Includes all required files:

  * `heart_app.py`
  * `logisticRegression_heart.pkl`
  * `scaler.pkl`
  * `columns.pkl`
  * `heart.csv` dataset (optional)



## ▶️ How to Run the Streamlit App

### **1️⃣ Install Dependencies**

Make sure Python 3.10 or 3.11 is installed.

Then install required libraries:

```bash
pip install streamlit pandas joblib scikit-learn
```

---

### **2️⃣ Run the App**

In the project folder, run:

```bash
streamlit run heart_app.py
```

Streamlit will open automatically in your browser at:

```
http://localhost:8501
```

---







